{
  "name": "angular-blog-api",
  "version": "0.0.0-development",
  "description": "Simple REST API for frontend code challenge",
  "scripts": {
    "api": "json-server --watch api/db.json --port 9001",
    "start": "npm run api"
  },
  "author": "Rafael Lopez Dorado",
  "license": "UNLICENSED",
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
